<!DOCTYPE html>
<html>
  <head>
    <title>Course Details</title>
  </head>
  <body>
    <div class="container">
      <h1>Course Details</h1>
      <p><strong>Course:</strong> <%= @course.course_name %></p>
      <p><strong>Amount:</strong> <%= @course.amount %></p>
      <p><strong>Description:</strong> <%= @course.description %></p>
      <p><%= link_to 'Back', admin_courses_path, class: "btn btn-secondary" %></p>
      <%= link_to "Sections", admin_course_sections_path(@course), class: "btn btn-primary" %>
    </body>
</html>
