<h1>Edit Section</h1>


  <%= form_for [:admin, @course, @section], url: admin_course_section_path( @course, @section) do |f| %>
    <div>
      <%= f.label :title %>
      <br>
      <%= f.text_field :title %>
    </div>
    <br>
    <div>
      <%= f.label :description %>
      <br>
      <%= f.text_area :description %>
    </div>
    <br>
      <%= f.submit "Update Section" %>
  
  <% end %>
<br>
  <%= link_to 'Back to Section List', admin_course_sections_path %>




