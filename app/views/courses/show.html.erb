<!DOCTYPE html>
<html>
<head>
  <title>Course Details</title>
</head>
<body>
  <h1>Course Details</h1>
  <p><strong>Course:</strong> <%= @course.course_name %></p>
  <p><strong>Description:</strong> <%= @course.description %></p>
  <% if current_user %>
  <%= link_to "Enroll", new_course_entrollment_path(course_id: @course.id), class: "btn btn-primary" %>
  <% end %>
  <%= link_to "Back to Course", courses_path(@course) %>

</body>
</html>
